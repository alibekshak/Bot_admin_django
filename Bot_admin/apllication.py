# import os
# from pathlib import Path

# INSTALLED_APPS = [
    
# ]

# BASE_DIR = Path(__file__).resolve().parent.parent

# DEBUG = os.getenv("DEBUG", cast=bool, default=False)

# LANGUAGE_CODE = os.getenv("LANGUAGE_CODE", cast=str, default="en-us")

# TIME_ZONE = os.getenv("TIME_ZONE", cast=str, default="UTC")

# USE_TZ = os.getenv("USE_TZ", cast=bool, default=True)

# DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"